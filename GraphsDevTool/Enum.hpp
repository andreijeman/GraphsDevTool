#ifndef ENUM_HPP
#define ENUM_HPP

enum state
{
	idle,
	hovered,
	pressed
};

enum userButton
{
	none = 0,
	mouseLeft,
	mouseRight,
	mouseMiddle,
	mouseScroll
};

enum command
{
	nothing = 0
};

#endif